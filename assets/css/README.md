# assets/css

Estilos compartilhados do front-end.

- `shell.css` — topbar, nav, tema (claro/escuro), badge de sincronização — usado por todas as páginas.
- Estilos específicos de cada página, quando existirem, ficam num `<style>` inline na própria página (mesmo padrão do `teste.html`) ou num arquivo próprio aqui, se crescer o suficiente pra justificar.
